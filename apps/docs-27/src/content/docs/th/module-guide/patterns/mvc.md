---
title: "MVC รูปแบบใน XOOPS"
description: "การใช้งานสถาปัตยกรรม Model-View-Controller ในโมดูล XOOPS"
---
<span class="version-badge version-xmf">XMF จำเป็น</span> <span class="version-badge version-40x">4.0.x Native</span>

:::note[ไม่แน่ใจว่านี่เป็นรูปแบบที่ถูกต้องหรือไม่]
ดู [การเลือกรูปแบบการเข้าถึงข้อมูล](../Choosing-Data-Access-Pattern.md) เพื่อดูคำแนะนำว่าเมื่อใดควรใช้ MVC เทียบกับรูปแบบที่ง่ายกว่า
::::::

:::caution[คำชี้แจง: XOOPS Architecture]
**มาตรฐาน XOOPS¤ 2.5.x** ใช้รูปแบบ **ตัวควบคุมเพจ** (หรือที่เรียกว่าสคริปต์ธุรกรรม) ไม่ใช่ MVC โมดูลเดิมใช้ `index.php` พร้อมกับการรวมโดยตรง อ็อบเจ็กต์ส่วนกลาง (`$xoopsUser`, `$xoopsDB`) และการเข้าถึงข้อมูลตามตัวจัดการ

**หากต้องการใช้ MVC ใน XOOPS 2.5.x** คุณต้องมี **XMF Framework** ซึ่งให้การสนับสนุนการกำหนดเส้นทางและตัวควบคุม

**XOOPS 4.0** จะรองรับ MVC โดยกำเนิดด้วย PSR-15 มิดเดิลแวร์และการกำหนดเส้นทางที่เหมาะสม

ดูเพิ่มเติม: [สถาปัตยกรรม XOOPS ปัจจุบัน](../../02-Core-Concepts/Architecture/XOOPS-Architecture.md)
::::::

รูปแบบ Model-View-Controller (MVC) เป็นรูปแบบสถาปัตยกรรมพื้นฐานสำหรับการแยกข้อกังวลในโมดูล XOOPS รูปแบบนี้แบ่งแอปพลิเคชันออกเป็นสามองค์ประกอบที่เชื่อมต่อถึงกัน

## MVC คำอธิบาย

### รุ่น
**รุ่น** แสดงถึงข้อมูลและตรรกะทางธุรกิจของแอปพลิเคชันของคุณ มัน:
- จัดการความคงอยู่ของข้อมูล
- ปฏิบัติตามกฎเกณฑ์ทางธุรกิจ
- ตรวจสอบข้อมูล
- สื่อสารกับฐานข้อมูล
- ไม่ขึ้นอยู่กับ UI

### ดู
**มุมมอง** มีหน้าที่นำเสนอข้อมูลแก่ผู้ใช้ มัน:
- เรนเดอร์เทมเพลต HTML
- แสดงข้อมูลโมเดล
- จัดการการนำเสนอส่วนต่อประสานกับผู้ใช้
- ส่งการกระทำของผู้ใช้ไปยังตัวควบคุม
- ควรมีตรรกะน้อยที่สุด

### ผู้ควบคุม
**ตัวควบคุม** จัดการการโต้ตอบของผู้ใช้และพิกัดระหว่างโมเดลและมุมมอง มัน:
- รับคำขอของผู้ใช้
- ประมวลผลข้อมูลอินพุต
- เรียกเมธอดโมเดล
- เลือกมุมมองที่เหมาะสม
- จัดการการไหลของแอปพลิเคชัน

## XOOPS การใช้งาน

ใน XOOPS รูปแบบ MVC ถูกนำมาใช้โดยใช้ตัวจัดการและเทมเพลต พร้อมด้วยกลไก Smarty ที่ให้การสนับสนุนเทมเพลต

### โครงสร้างโมเดลพื้นฐาน
```php
<?php
class UserModel
{
    private $db;
    
    public function getUserById($id)
    {
        // Database query implementation
    }
    
    public function createUser($data)
    {
        // Create user implementation
    }
}
?>
```
### การใช้งานคอนโทรลเลอร์
```php
<?php
class UserController
{
    private $model;
    
    public function listAction()
    {
        $users = $this->model->getAllUsers();
        return ['users' => $users];
    }
}
?>
```
### ดูเทมเพลต
```smarty
{foreach from=$users item=user}
    <div>{$user.username|escape}</div>
{/foreach}
```
## แนวทางปฏิบัติที่ดีที่สุด

- เก็บตรรกะทางธุรกิจไว้ในโมเดล
- เก็บการนำเสนอใน Views  
- ติดตามเส้นทาง/ประสานงานในคอนโทรลเลอร์
- อย่าผสมความกังวลระหว่างชั้น
- ตรวจสอบอินพุตทั้งหมดในระดับคอนโทรลเลอร์

## เอกสารที่เกี่ยวข้อง

ดูเพิ่มเติมที่:
- [รูปแบบพื้นที่เก็บข้อมูล](../Patterns/Repository-Pattern.md) สำหรับการเข้าถึงข้อมูลขั้นสูง
- [Service-Layer](../Patterns/Service-Layer.md) สำหรับตรรกะทางธุรกิจที่เป็นนามธรรม
- [รหัส-องค์กร](../Best-Practices/Code-Organization.md) สำหรับโครงสร้างโครงการ
- [การทดสอบ](../Best-Practices/Testing.md) สำหรับ MVC กลยุทธ์การทดสอบ

---

Tags: #mvc #รูปแบบ #สถาปัตยกรรม #การพัฒนาโมดูล #รูปแบบการออกแบบ